{
  "buildCommand": "mkdir -p dist && cp blueprint-site/index.html dist/index.html && cp -r public/* dist/ 2>/dev/null || true",
  "outputDirectory": "dist",
  "framework": null,
  "regions": ["iad1"]
}
